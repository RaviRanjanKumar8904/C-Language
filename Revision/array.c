//Create an integer array of size 5. Initialize it with numbers {10, 20, 30, 40, 50} and print the element at index 3.

#include <stdio.h>
int main()
{
    int arr[5] = {10,20,30,40,50};

    //print the number which at index 3.
    printf("Number at index 3 is %d",arr[3]);
    return 0;
}